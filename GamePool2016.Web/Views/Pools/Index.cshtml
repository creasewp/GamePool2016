@model IEnumerable<GamePool2016.Data.Pool>

@{
    ViewBag.Title = "Pools";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Pools</h2>

<div class="row">
    <div class="col-md-4">

        <li>@Html.ActionLink("Create", "Create", "Pools") a new pool</li>
        <li>@Html.ActionLink("Join", "JoinPublic", "Pools") a public pool</li>
        <li>@Html.ActionLink("Join", "JoinPrivate", "Pools") a private pool</li>
    </div>
</div>


@*<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsPublic)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsPublic)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }

    </table>
*@